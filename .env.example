# Koda — Environment Variables
#
# Copy to .env and fill in your API keys.
# Bun auto-loads .env at startup.
#
# All other settings (models, agent config, timezone, etc.)
# go in config.json — see config/config.example.json.

# Required
KODA_OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Telegram (required unless mode is cli-only)
KODA_TELEGRAM_TOKEN=123456789:your-telegram-bot-token

# Optional — web search (get free key at https://dashboard.exa.ai/api-keys)
KODA_EXA_API_KEY=your-exa-api-key

# Optional — override embedding model (default: openai/text-embedding-3-large)
# KODA_MEMORY_EMBEDDING_MODEL=openai/text-embedding-3-large
